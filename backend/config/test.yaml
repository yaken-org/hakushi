server:
  host: 0.0.0.0
  port: 8080

database:
  host: ${DB_HOST:localhost}
  port: ${DB_PORT:3306}
  database: ${DB_NAME:hakushi_test}
  username: ${DB_USER:hakushi}
  password: ${DB_PASSWORD:hakushi}
  timezone: Asia/Tokyo
  maxIdleConns: 10
  maxOpenConns: 100
  connMaxLifetime: 300